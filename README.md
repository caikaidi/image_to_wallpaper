# image_to_wallpaper

一个前端小工具：上传任意壁纸，自动生成类似 iOS 景深风格的预览图，并支持导出。

## 功能

- 上传图片后自动生成背景模糊层。
- 前景会生成多个「切片窗口」，数量可调。
- 每个窗口都支持鼠标/触控拖拽调整位置。
- 可实时调节背景模糊强度并重置窗口布局。
- 一键导出当前效果为 PNG。

## 本地运行

```bash
python3 -m http.server 4173
```

然后访问：`http://localhost:4173`
